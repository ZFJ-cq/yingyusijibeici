# 英语四级遗忘曲线背单词（Vue3 纯前端应用）

一个**无需后端、不使用数据库**的背单词单页应用：所有学习数据都保存在浏览器
`localStorage` 中。基于遗忘曲线安排复习，卡片式记忆。

界面为**移动端 App 风格**：顶部精简标题栏 + **底部 Tab 栏切换**
（看板 / 复习 / 练习 / 统计 / 设置），适配 iPhone 刘海与底部安全区，
也能在电脑上居中显示（内容限宽 680px，形成「App 视口」观感）。

> 「学新词」不占底部 Tab：它是从**首页看板**进入的单独页面，带返回按钮，
> 属于「用完即走」的操作型页面，避免和日常主入口抢位置。

## ✨ 功能特性

| 页面 | 入口 | 说明 |
| --- | --- | --- |
| **首页看板** | Tab | 今日主卡片（智能提示该复习还是学新词）、「待复习 / 新词 / 已掌握」数量、总体进度、今日已复习、三个入口 |
| **学新词** | 首页入口 | **可选词书（常规词汇 / 高频词汇）**；先只显示单词与音标（**点「显示释义」才展开中文释义与例句**，保留自己回想的环节）；【认识 / 不认识】；可自定义每次学习数量；本批进度条与「结束本批」 |
| **复习** | Tab | 可翻转卡片（正面英文，点击翻面显示释义）；显示该词处于**第几次复习节点**与下次间隔；【记得 / 不记得】；「结束复习」后展示本次统计 |
| **练习** | Tab | 独立刷题模式：**九种题型**（英译中 / 中译英 / 短句填空 / 默写填空 / 蒙版遮挡 / 词组填空 / 词组四选一 / 选词填空·真题 / 汉译英·真题）；**不影响遗忘曲线**；结束汇总正确率与错题，错题一键加入生词本 |
| **统计** | Tab | **学习概览（今日学习 / 累计学习 / 今日时长 / 累计时长）** + 简易图表（状态分布、近 7 天复习活跃度、练习统计） |
| **设置** | Tab | 出词顺序、每次学习数量、记录导出/导入、清空生词本、重置练习统计、清空学习时长、重置记录、主题切换、朗读开关与语速 |

**其它**
- 使用 **Web Speech API** 实现单词朗读发音（无需网络、无第三方依赖）。
- 单词**默认乱序**出题，避免按字母序死记；可在设置中切回**正序**。
- 提示与确认使用**应用内 Toast / 弹窗**（不用原生 `alert/confirm`）：样式与主题统一，
  且在 iOS「添加到主屏」独立运行等场景下不会被浏览器忽略。
- 全部图表用原生 div + CSS 绘制，不引入任何图表库。
- 键盘可达：复习卡片支持 <kbd>Space</kbd> / <kbd>Enter</kbd> 翻面。

## 🔁 遗忘曲线规则（SRS）

- 学完一个**新词**后，自动进入复习计划，复习节点依次为：**1 天 → 2 天 → 4 天 → 7 天 → 15 天**。
- 复习时点击**【记得】**：进入下一个复习节点；完成全部 5 个节点后标记为**已掌握**。
- 复习时点击**【不记得】**：**重置该单词的学习进度，从头开始**（重新回到 1 天节点）。
- 时间统一使用**毫秒时间戳**，前端计算时间差判断是否到达复习时间。

> ⚠️ **硬约束：只有【复习模块】会写 SRS 进度。**
> 【练习模式】无论答对答错，都**不会修改任何单词的复习时间戳**——
> 它只把错题记入「生词本」并累加练习统计。参见 `useStore.js` 中
> `learnWord / reviewRemember / reviewForget` 与 `recordPractice / addToNotebook` 的边界注释。

## ✍️ 练习模式（独立刷题，不影响 SRS）

### 九种题型

| 题型 | 交互 | 需要打字 |
| --- | --- | --- |
| **英译中** | 展示英文单词（含音标与朗读按钮）→ 输入框填写中文释义 → 提交判分并展示标准答案 | 是 |
| **中译英** | 展示中文释义 → 输入英文单词 → 支持模糊匹配（忽略大小写与空格）与**拼写容错** | 是 |
| **短句填空** | 挖空四级例句中的目标单词，**括号给出原形提示**：`How could she ______ her own child? (abandon)` | 是 |
| **默写填空** | 比短句填空更难：给中文释义 + 挖空例句，**不给原形提示**，纯靠记忆默写；卡住时可点「听发音提示」 | 是 |
| **蒙版遮挡** | 题面正常显示、答案被蒙版盖住（磨砂 + 斜纹），先在心里回忆 → 点开蒙版核对 → **自评「记住了 / 没记住」**。可选遮挡方向：遮中文（看词猜义）/ 遮英文（看义想词） | 否 |
| **词组填空** | 给中文释义 + 挖空的固定搭配 → 填出搭配里缺的那个词：`考虑到 → take into ______` | 是 |
| **词组四选一** | 给中文释义 → 四个真实搭配里选正确的，干扰项取自**其他单词**的真搭配 | 否 |
| **选词填空·真题** ⭐ | **四级真题原文**挖 10 个空，从 15 个候选词里选（每词限用一次），逐空判分 | 否 |
| **汉译英·真题** ⭐ | **四级真题翻译原句**译成英文 → 按关键词覆盖率判分，并给出参考译文 | 是 |

> ⭐ 标记的两种题型用的是**真实四级真题**（`src/data/cet4Exam.js`），不依赖词池：
> 现有 **6 篇**真题选词填空（含原文 / 15 词词库 / 标准答案）与 **9 句**真题翻译（含参考译文与判分关键词）。

- 题型可选**混合（推荐）**或单独练习某一种。
  混合 = 「英译中 / 中译英 / 短句填空 / 默写填空 / 词组填空」中随机出（每次作答都有客观判分）；
  **蒙版遮挡是自评题型，不参与「混合」**，需要单独选择它。
- **拼写容错**规则：≤3 字母必须完全正确；4-8 字母允许差 1 个字符（少写一个 `s` 这类笔误会被判对并提示）；
  ≥9 字母允许差 2 个字符。差距超出容错但只差一点时，会提示「很接近，注意拼写」。
- 短句填空与默写填空都只在例句中出现**单词原形**时出题（否则挖掉变形词却要求填原形会产生歧义），
  并过滤掉虚词（长度 < 3）与例句片段。若所选词池里一个可出题的词都没有，
  会提示「该词池里没有可出此题型（需要例句）的单词」，而不是出一组空题。
- **蒙版遮挡没有客观答案**，由你自评对错。自评结果只进本次练习统计，同样不写 SRS。
  实现上对误用做了硬防护：若代码里对蒙版题调用客观判分函数会**直接抛错**，
  避免把「看词猜义」静默判成「拼写对不对」。
- **汉译英没有唯一正确答案**，因此用**关键词覆盖率**判定：命中 ≥60% 关键词即算通过，
  并**始终**给出参考译文与未覆盖的关键词，由你自己对照自查。
  这里不假装机器能精确评判翻译。
- **词组题的题源**是高频词常用搭配整理（`src/data/highFreqPhrases.js`，608 词 / 1549 条），
  属教学向整理，**不是真题**；真题只出现在「选词填空·真题」与「汉译英·真题」两种题型里。
- 选词填空的**每个空单独计一条记录**，正确率按"空"统计，答错的词同样可在结果页加入生词本。

### 英译中的判分规则

判分逻辑在 `src/utils/practice.js` 的 `judgeMeaning()`，只做**中文语义比对**，三步：

1. **归一化**：用户输入与标准释义都先去掉空格与标点（中英文逗号/分号/顿号/括号等）、统一小写，
   避免全半角与空格造成误判。
2. **切义项**：标准释义去掉词性前缀（`n.` `v.` `adj.` …）后，按 `，,；;、/|` 切成义项列表。
   - `v. 丢弃；放弃，抛弃` → `丢弃` / `放弃` / `抛弃`
   - `adj. 印度的；n. 印度人` → `印度的` / `印度人`
3. **逐条比对，命中任意一条即判对**：
   - 完全相等 → 对
   - 用户输入是某义项的子串（输入 ≥ 2 字）→ 对。例：答「放弃」命中「放弃，抛弃」
   - 某义项是用户输入的子串（义项 ≥ 2 字）→ 对。例：答「放弃，抛弃」包含完整义项
   - **单字只允许完全相等**，否则「的」会命中一大片词

对 3739 词全量的实测结果：

| 检查项 | 结果 |
| --- | --- |
| 每个原始义项单独作答 | **100% 判对**（0 例失败） |
| 明显无关的答案（「跑得快」「明天见」「一本书」） | 误判 **0 次** |
| 略沾边的答案（「计算机系统」含「计算」） | 最多 5 个词误判（约 0.13%） |
| 输入单个汉字 | 「的」「大」→ 0 词；「人」「不」只命中义项本身即该字的词 |

取向是**偏宽松**：答出核心含义（哪怕只写两个字）就算对——这题是自测巩固用的，
判太严反而打击积极性；它只拒绝**明显不相关**的答案。

### 配置项

- **练习词池**：全部单词 / 仅待复习 / 仅已掌握 / 生词本（各自显示可用词数）
- **练习数量**：5 / 10 / 20 题，或自定义 1-100
- **题型**：混合 / 英译中 / 中译英 / 短句填空 / 默写填空 / 蒙版遮挡 / 词组填空 / 词组四选一 /
  选词填空·真题 / 汉译英·真题（选中蒙版后可再选遮挡方向）

### 结束与错题

- 汇总**正确率 / 答对 / 答错**。
- 列出**错题清单**（题型、题面、你的答案、正确答案），支持**一键把全部错题加入生词本**，
  也可逐个加入；生词本中的单词可作为词池做专项练习。
- 生词本可在「练习」页展开查看/移除，也可在「设置 → 清空生词本」整体清空。

## 🗂 目录结构

```
英语四级遗忘曲线背单词/
├── index.html                 # 应用入口 HTML（含移动端 viewport / 安全区配置）
├── package.json               # 依赖与脚本
├── vite.config.js             # Vite 配置（相对路径 base，支持 GitHub Pages）
├── README.md
├── public/
│   └── .nojekyll              # GitHub Pages 用：跳过 Jekyll 处理
├── .github/
│   └── workflows/
│       └── deploy.yml         # GitHub Actions：自动构建并部署到 Pages
└── src/
    ├── main.js                # 创建应用、挂载路由与全局样式
    ├── App.vue                # App 外壳：顶部标题栏 + 内容区 + 底部 Tab 栏
    ├── router/
    │   └── index.js           # 路由（hash 模式，静态托管友好）
    ├── data/
    │   ├── index.js           # 词书合并层：两本词书按单词去重合并 + 按词书取词
    │   ├── words.js           # 常规词库 3739 词（word / phonetic / meaning / example / exampleCn）
    │   ├── highFreqWords.js   # 高频词库 608 词（84 词自带音标/释义/例句 + 524 词复用常规词条）
    │   ├── highFreqPhrases.js # 高频词「词组/搭配」608 词 / 1549 条（词组题题源，教学向整理）
    │   └── cet4Exam.js        # 四级真题语料：6 篇选词填空 + 9 句真题翻译 + 30 条真题例句
    ├── composables/
    │   ├── useStore.js        # 核心状态：遗忘曲线 + 洗牌乱序 + 生词本 + 练习统计 + 时长记账 + 持久化
    │   ├── useStudyTimer.js   # 学习时长计时（活跃判定 + 空闲暂停 + 定时落盘 + 跨天归档）
    │   ├── useUI.js           # 全局 Toast / 应用内确认弹窗
    │   └── useSpeech.js       # Web Speech API 朗读工具
    ├── utils/
    │   ├── practice.js        # 练习引擎（纯函数）：判分、模糊匹配、挖空、组题
    │   └── format.js          # 时长等展示格式化
    ├── components/
    │   ├── WordCard.vue       # 单词卡片（学习模式 / 可翻转复习模式）
    │   └── AppUI.vue          # 渲染 Toast 与确认弹窗
    ├── views/
    │   ├── Home.vue           # 首页看板
    │   ├── Learn.vue          # 学新词
    │   ├── Review.vue         # 复习
    │   ├── Practice.vue       # 练习模式
    │   ├── Stats.vue          # 统计
    │   └── Settings.vue       # 设置
    └── styles/
        └── global.css         # 设计令牌 + App 外壳布局 + 浅色/暗黑主题
```

## 🚀 本地运行

> 需要 Node.js 16+（推荐 18 / 20）。

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器（默认 http://localhost:5173，自动打开浏览器）
npm run dev

# 3. 构建生产版本（产物在 dist/）
npm run build

# 4. 本地预览构建结果
npm run preview
```

## 🌐 部署到 GitHub Pages

### ⚠️ 最容易踩的坑：把源码当成构建产物发布

本项目的**源码 `index.html` 引用的是 `/src/main.js`**（开发入口）。
若把源码直接放到 Pages 上，浏览器会去请求 `https://<user>.github.io/src/main.js`：

```
Failed to load resource: the server responded with a status of 404 ()   main.js
```

结果就是**页面标题正常、但整页白屏**（`<div id="app">` 为空，Vue 从未挂载）。

> **自检方法**：打开线上页面按 F12，看 `<div id="app">` 是否为空；
> 或直接请求 `你的站点路径/src/main.js`，若 404 就说明发布的是**源码**。
> 正确部署后，`index.html` 引用的是 `./assets/index-xxxx.js` 这类**构建产物**。

**结论：Pages 上只放 `dist/` 的内容（`index.html` + `assets/` + `.nojekyll`），不要放 `src/`、`package.json` 等源码。**

### 方式一：GitHub Actions 自动构建（推荐）

项目已内置工作流 `.github/workflows/deploy.yml`，会自动探测项目位置并构建发布：

| 源码位置 | 站点地址 |
| --- | --- |
| 仓库根目录 | `https://<user>.github.io/<repo>/` |
| `beici/` 子目录 | `https://<user>.github.io/<repo>/beici/` |

> 部署路径与源码所在目录**保持一致**，避免「源码在 `/repo/beici`、站点却在 `/repo`」的错位。

步骤：

1. 把本项目推送到仓库 `main` 分支（放根目录或 `beici/` 子目录都行）：
   ```bash
   git init
   git add .
   git commit -m "feat: 四级遗忘曲线背单词"
   git branch -M main
   git remote add origin https://github.com/<用户名>/<仓库名>.git
   git push -u origin main
   ```
2. 仓库 **Settings → Pages** → **Build and deployment → Source** 改为 **GitHub Actions**。
   **关键一步**：若保留默认的 "Deploy from a branch"，Pages 会直接发布你的**源码**，就会白屏。
3. 等 Actions 跑完（仓库 **Actions** 页签看进度），访问上表中的地址。

### 方式二：手动部署构建产物（不依赖 Actions）

```bash
npm run build          # 产物在 dist/
```

把 **`dist/` 里的内容**（`index.html`、`.nojekyll`、`assets/` 整个目录）复制到仓库对应目录，
**替换掉原来的源码文件**，并保持 Pages 的 Source = "Deploy from a branch"。
例如要发布到 `https://<user>.github.io/<repo>/beici/`：

```bash
# 在仓库根目录执行
rm -rf beici && mkdir -p beici && cp -r /path/to/dist/. beici/
git add -A && git commit -m "deploy: 发布构建产物" && git push
```

> ⚠️ `beici/` 里必须**只有构建产物**。若 `src/`、`package.json` 还留在那里，
> 或 `index.html` 仍是源码版本，都会继续白屏。源码建议单独放在仓库其它目录。

### 为什么子路径下也能正常工作

- 应用使用 **hash 路由**（地址形如 `.../#/review`），刷新任意子页面都由 `index.html` 承接，**不会 404**。
- 资源引用使用**相对路径**（`vite.config.js` 的 `base` 默认 `'./'`），
  因此放到 `/repo/`、`/repo/beici/` 等任意子路径都能正确加载，无需改代码。

## 📖 如何使用

通过**底部 Tab 栏**在看板 / 复习 / 练习 / 统计 / 设置之间切换（与手机 App 一致），
「学新词」从**首页看板**的入口进入。

1. **学新词**（首页进入）：设置每次学习数量 → 逐张卡片点击【认识】或【不认识】。
   - 【不认识】的词会排到本批队尾，稍后重学；
   - 【认识】的词加入复习计划，1 天后首次复习。
2. **复习**：到期的单词会出现在复习页，先凭记忆判断，**点击卡片可翻面**看释义。
   - 【记得】进入下一节点；【不记得】重置进度从头再来；
   - 结束展示本次「记得 / 不记得」统计。
3. **练习**：选词池 + 题量 + 题型 → 逐题作答 → 汇总正确率与错题 → 错题可加入生词本。
   - 这是**独立刷题**，答错不会影响遗忘曲线的复习时间。
4. **统计**：查看整体进度、近 7 天复习趋势与练习统计。
5. **设置**：切换主题与出词顺序、调整每次学习数量与朗读、导出 / 导入备份、
   清空生词本、重置练习统计、重置学习记录。

> 💡 **乱序**：默认从词库中随机抽词，避免按字母序背诵。若想按字母顺序复习，
> 到「设置 → 出词顺序」切换为「正序」即可。

> 💡 **建议用法**：先「学新词」建立计划 → 每天按时「复习」推进遗忘曲线 →
> 碎片时间用「练习」刷题巩固；练习答错的词进了生词本后，可用「生词本」词池做专项突破。

## 📚 关于词库数据

应用内置**两本词书**，在「学新词 → 选择词书」里切换：

| 词书 | 词数 | 来源 | 说明 |
| --- | --- | --- | --- |
| **常规词汇** | 3739 | 有道四级词汇（正序版） | 覆盖四级大纲词汇，含音标 / 释义 / 例句 / 译文 |
| **高频词汇** | 608 | 用户提供的《四级高频词最新版.pdf》 | 四级高频重点词；来自 18 页纯图片 PDF，已与用户提供的词表逐词校对 |

### 两本词书怎么共存

关键点：**两个词书以"单词"为唯一键合并成同一个词条池**（`src/data/index.js`），
同一个单词只有一条记录，用 `lists` 标记它属于哪几本词书：

```js
{ word: 'abandon', phonetic: "/ə'bændən/", meaning: 'v. 丢弃；放弃，抛弃',
  example: 'How could she abandon her own child?', exampleCn: '她怎么能抛弃自己的孩子？',
  lists: ['regular', 'high'] }
```

这么设计是**必须的**，因为：

- 学习进度（SRS）以单词为键存储，一个词不能有两份含义、两套进度；
- 复习页 / 练习页 / 统计页都基于同一个词条池工作。若两本词书各自独立，
  从高频词书学到的词在复习页会**找不到、永远无法复习**。

并集规模：3739 + 608 − 524（重合）= **3823 词**。其中：

- **524 个词**两本词书都有 → 复用常规词条的音标 / 释义 / 例句，保证同一个词在两处显示一致；
- **84 个词**仅高频词表有 → 用 PDF 里的释义，并已补齐**音标与例句**（见下方说明）。

### 高频词表是怎么核对与补全的

- **逐词校对**：以你提供的词表文件为基准逐词比对，608 个词里 606 个与视觉识别结果完全一致，
  修正了 2 处误读（`liable` 曾被误读成 `lodge`、`according` 曾被写成短语 `according to`）。
- **词形校验**：用 macOS 系统词典（`/usr/share/dict/words`，23.6 万词）逐个核验，
  608 个词里只有 1 个（`cooperation`）不在该词典中，它本身也是合法英文词。
- **补例句与音标**：PDF 原表只有「词 + 词性 + 中文释义」，**没有任何例句**。
  为让卡片可用，给缺失的 90 个词（84 个高频独有词 + 6 个原本缺例句的重合词）补了
  音标与例句；顺带修正了 `reception` 的词性标注（原为 `adj.`，实为 `n.`）。
  现在**两本词书的每个词都有音标和例句**，练习时不会出现「没有例句不能出填空」的死角。

> ⚠️ **这些例句是人工撰写的**（自然、贴合释义），**不是词典或语料库原文**，
> 仅供学习参考，不代表权威用法。这一点在 `src/data/highFreqWords.js` 的文件头也写明了。

### 词组（常用搭配）与真题例句

高频词（608 个）除了音标 / 释义 / 例句，还额外挂了两样内容，直接显示在单词卡片上：

| 内容 | 覆盖 | 来源 | 用途 |
| --- | --- | --- | --- |
| **常用搭配** | 608 词 / **1549 条** | `src/data/highFreqPhrases.js`，教学向整理 | 卡片展示；「词组填空 / 词组四选一」题源 |
| **真题例句** | 30 词 / 30 条 | `src/data/cet4Exam.js`，**真实四级真题原句** | 卡片展示，标注真题出处 |

- 搭配只挑**考试真会考**的：动介搭配（`account for`）、形介搭配（`sensitive to`）、
  固定词组（`take into account`）、介词短语（`on account of`）等，每个词 2~3 条。
- **真题例句来自真题阅读与选词填空原文**，并标注年份与题型
  （如「2026年6月真题·选词填空」「2014年12月真题·选词填空」），与上面的词典例句是两回事。
- 只有高频词有这两样；**常规词不受影响**（3215 个纯常规词不会多出任何字段）。

> ⚠️ 请区分：**搭配是整理的教学内容，真题例句才是真题原文。**
> 卡片上两者用了不同的标签与配色，避免混淆。

> ⚠️ 关于高频词表的准确性：该 PDF 是**纯图片、无文本层**，我逐页视觉识别后用
> macOS 系统词典（23.6 万词）校验了全部词形 —— 608 个词里只有 1 个（`cooperation`）
> 不在该词典中，它本身也是合法英文词。另：PDF 第 16–18 页与第 12–14 页内容重叠，已去重。

如需换成其他版本词表，替换对应数据文件并保持相同字段结构即可，其余代码无需改动。


## ⏱ 学习时长是怎么统计的

统计页的「今日时长 / 累计时长」由 `src/composables/useStudyTimer.js` 记录，
**不是"页面开着就计时"**：

- 监听点击 / 按键 / 滚动 / 触摸 / 滚轮等交互事件，每次交互都记一次"活跃"；
- 每秒一次心跳，只有距上次交互在 **1 分钟**以内才 +1 秒；
- 超过 1 分钟没有任何操作 → 认为人不在，**暂停计时**（避免挂着页面刷时长）；
- 累计的秒数先在内存里攒着，每 **15 秒**批量写回一次（减少 localStorage 写入），
  并在页面切到后台 / 关闭时立即落盘，防止丢数据。

跨天会自动归档：零点后第一次落盘时，把昨天的"今日时长"写进每日明细，
再从 0 重新算今天，所以「今日时长」不会把昨天的算进来。

定义口径：

| 指标 | 口径 |
| --- | --- |
| **今日学习** | 今天**新学或复习过**的单词数（去重） |
| **累计学习** | 已学过的单词总数（= 学习中 + 已掌握） |
| **今日时长 / 累计时长** | 按上面的"活跃时间"累计 |

> 「清空学习时长」在 **设置 → 数据管理**，只把时长归零，不影响单词进度与复习计划。

## 💾 数据存储

- 存储位置：浏览器 `localStorage`，键名为 `cet4_forgetting_curve_v1`。
- 存储内容：
  - `progress`：每词的 SRS 进度 `state / learnedAt / reviewsDone / nextReviewAt / lastReviewAt / history`
    （**只有复习模块会写**）
  - `notebook`：生词本 `{ [单词]: { addedAt } }`
  - `practice`：练习统计 `{ sessions, answered, correct }`
  - `study`：学习时长记账 `{ totalSeconds, todayDate, todaySeconds, dailySeconds }`
    （只保留最近 14 天的每日明细，避免 localStorage 无限增长）
  - `settings`：主题、每次学习数量、出词顺序、词书、朗读等偏好
- ⚠️ 清除浏览器数据会一并清除学习记录，建议定期在**设置**页导出备份。

## 🛠 技术栈

Vue 3（`<script setup>` 组合式 API）· Vue Router（hash 模式）· Vite · 原生 CSS（CSS 变量主题）· Web Speech API
